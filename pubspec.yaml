name: asuna_video_player
description: A new video player plugin.
version: 0.2.2
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  screen: ^0.0.5
  simple_gesture_detector: ^0.1.3
  logging: ^0.11.3+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    androidPackage: danielwii.github.io.asuna_video_player
    pluginClass: AsunaVideoPlayerPlugin
